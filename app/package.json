{
  "name": "node-file-transfer",
  "version": "1.0.0",
  "description": "Share files between clients without storing them on the server",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Christian Hagdorn",
  "license": "ISC",
  "dependencies": {
    "express": "^4.13.3",
    "socket.io": "^1.3.7",
    "socket.io-stream": "^0.9.0"
  }
}
